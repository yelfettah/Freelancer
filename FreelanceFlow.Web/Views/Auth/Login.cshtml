@{
    ViewData["Title"] = "Giriş Yap";
}

<section class="py-5 bg-light-blue min-vh-100 d-flex align-items-center">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12" style="max-width: 450px;">
                <div class="card border-0 shadow-sm p-4">
                    <h2 class="fw-bold mb-3 text-center">Giriş Yap</h2>

                    @if (!string.IsNullOrWhiteSpace(ViewBag.Error))
                    {
                        <div class="alert alert-danger">@ViewBag.Error</div>
                    }

                    <form method="post" asp-action="Login" asp-controller="Auth">
                        <div class="mb-3">
                            <label for="email" class="form-label fw-semibold">E-posta</label>
                            <input type="email" class="form-control" id="email" name="email" required />
                        </div>
                        <div class="mb-4">
                            <label for="password" class="form-label fw-semibold">Şifre</label>
                            <input type="password" class="form-control" id="password" name="password" required />
                        </div>
                        <button type="submit" class="btn btn-accent w-100">Giriş Yap</button>
                    </form>

                    <p class="text-center mt-3 mb-0">
                        Hesabınız yok mu?
                        <a asp-controller="Auth" asp-action="Register" class="fw-semibold text-primary-brand">Kayıt Ol</a>
                    </p>
                </div>
            </div>
        </div>
    </div>
</section>
